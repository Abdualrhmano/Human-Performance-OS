# --- Core API / Web ---
fastapi==0.109.0
uvicorn==0.27.0
pydantic[email]==2.6.0
python-multipart==0.0.6
jwt

# --- Auth / Security ---
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
cryptography==42.0.2

# --- Redis / Scheduling / Env ---
redis==5.0.0
apscheduler==3.10.1
python-dotenv==1.0.0

# --- Google Gemini AI ---
google-generativeai==0.3.2

# --- Data Science & ML (The Full Package) ---
numpy==1.26.3
pandas==2.2.0
scipy==1.12.0
scikit-learn==1.3.2
joblib==1.3.2
lightgbm==4.4.0

# --- ONNX / Conversion (optional for model export) ---
#onnx==1.15.0
skl2onnx==1.20.0

# --- Fixes / Compatibility
# (pin a stable huggingface_hub to avoid known utils import issues)
huggingface_hub>=0.20.0

# --- Async DB / HTTP ---
aiosqlite==0.19.0
aiohttp==3.9.3
requests==2.31.0

# --- Frontend & Visualization ---
streamlit>=1.40.0
plotly==5.18.0
Pillow==10.0.0
websocket-client==1.6.3
watchdog==4.0.0

# --- BLE (optional) ---
bleak==0.21.1

# --- DB Drivers / Production ---
psycopg2-binary==2.9.10
gunicorn==20.1.0
fakeredis
# --- Monitoring / Observability ---
prometheus-client==0.17.0
sentry-sdk==1.24.0

# --- Utilities (dev / tooling) ---
jq==1.6

# --- Notes
# - If you don't use Redis in production, you can omit redis and related worker components.
# - If you will not export models to ONNX, onnx/skl2onnx are optional.
# - Keep GEMINI_KEY in environment variables; do NOT hardcode it.
