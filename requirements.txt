# Core API / Web
fastapi==0.109.0
uvicorn==0.27.0
pydantic[email]==2.6.0
python-multipart==0.0.6

# Auth / Security
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
cryptography==42.0.2

# Redis / scheduling / env
redis==5.0.0
apscheduler==3.10.1
python-dotenv==1.0.0

# AI client (optional)
google-generativeai==0.3.2

# Data science / ML
numpy==1.26.3
pandas==2.2.0
scipy==1.12.0
scikit-learn==1.3.2
lightgbm==4.4.0
joblib==1.3.2

# Async DB / HTTP
aiosqlite==0.19.0
aiohttp==3.9.3
requests==2.31.0
redis
# Hardware / BLE
bleak==0.21.1

# Frontend / visualization
streamlit>=1.40.0
plotly==5.18.0
watchdog==4.0.0
Pillow==10.0.0

# DB adapters / production
psycopg2-binary==2.9.10
gunicorn==20.1.0

# Observability
prometheus-client==0.17.0
sentry-sdk==1.24.0

# ONNX / inference acceleration
onnx==1.15.0
# onnxruntime is environment-specific; install via conda/micromamba or uncomment if you know your Python version supports it
# onnxruntime==1.16.1

# Optional conversion helpers
# Use a skl2onnx version compatible with scikit-learn 1.3.x
skl2onnx==1.20.0

# WebSocket client for Streamlit background thread
websocket-client==1.6.3

# Utilities / dev
pytest==7.4.0
jupyterlab==4.3.0
